🔹 1. api/ → llamadas al backend (MUY importante)

Esta carpeta es clave en cualquier proyecto serio.

👉 Qué es

Son archivos JS que se encargan exclusivamente de comunicarse con tu backend (Spring Boot).

NO tienen lógica de UI.
NO tocan el DOM.
SOLO hacen fetch o axios.